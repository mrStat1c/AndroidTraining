<?xml version="1.0" encoding="utf-8"?><!--layout для отображения элемента списка (внутри RecycleView)-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

<!--    будет отображать число -->
    <TextView
        android:id="@+id/tv_number_item"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|start"
        android:fontFamily="monospace"
        android:textSize="27sp" />

<!--    будет отображать index элемента ViewHolder -->
    <TextView
        android:id="@+id/tv_view_holder_number"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:fontFamily="monospace"
        android:textColor="#000"
        android:textSize="27sp" />
</FrameLayout>